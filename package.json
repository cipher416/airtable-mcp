{
  "name": "airtable-mcp",
  "version": "0.1.0",
  "description": "An xmcp application with tool syntax",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "xmcp build",
    "dev": "xmcp dev",
    "start": "node dist/http.js",
    "lint": "tsc --noEmit",
    "prepare": "husky"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "xmcp": "0.1.13",
    "zod": "3.24.4"
  },
  "devDependencies": {
    "@types/minimatch": "^6.0.0",
    "swc-loader": "^0.2.6"
  }
}
